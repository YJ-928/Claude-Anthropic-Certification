# Create a CLI based chat bot
## Create MCP Client and Server using FastMCP
### Run the main.py to interact with bot which in-turn interacts with the mcp client with user request and communicates with the mcp server and returns the expected results.